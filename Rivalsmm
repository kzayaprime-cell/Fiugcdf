local ok, err = pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/kzayaprime-cell/Fiugcdf/refs/heads/main/Rivalsmm"))()
end)

if not ok then
    warn("=== MOKA ERROR ===")
    warn(tostring(err))
    warn("=== END ===")
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Moka Error",
        Text = tostring(err):sub(1, 200),
        Duration = 10,
    })
else
    print("Moka loaded OK")
end
